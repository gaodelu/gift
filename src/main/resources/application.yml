server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gift?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT
    username: root
    password: Gao303664805
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    max-active: 200
    max-wait: -1
    max-idle: 10
    min-idle: 0
    timeout: 1000


mybatis:
  mapper-locations: classpath:static/mapper/*Mapping.xml
  type-aliases-package: com.mochi.springboot.gift.*

